pub mod constants {
    pub const TIME_BETWEEN_MOVES: f64 = 0.20f64;
    pub const WINDOW_SIZE: u32 = 1000;
    pub const GRID_SIZE: u32 = 25;
    pub const SNAKE_SIZE: f64 = WINDOW_SIZE as f64 / GRID_SIZE as f64;
}
